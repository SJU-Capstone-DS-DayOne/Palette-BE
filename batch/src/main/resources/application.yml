spring:
    application:
        name: palette-batch
    profiles:
        include:
            - db
            - mongodb
            - rabbitmq
            - jwt
    jpa:
        show-sql: false
        properties:
            hibernate:
                format_sql: false
                highlight_sql: true
    batch:
        jdbc:
            initialize-schema: always
        job:
            enabled: false